<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - ScentOS</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary-dark: #1a0f0a;
      --brown-dark: #2d1810;
      --brown-medium: #462506;
      --brown-light: #5D4037;
      --gold-dark: #9F6920;
      --gold-medium: #DAA755;
      --gold-light: #E9C579;
      --gold-lightest: #F1DEA8;
      --text-light: #ffffff;
      --text-dark: #2d1810;
      --shadow-gold: 0 4px 20px rgba(218, 167, 85, 0.15);
      --shadow-strong: 0 8px 30px rgba(26, 15, 10, 0.3);
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      min-height: 100vh;
      background: linear-gradient(135deg,
        var(--primary-dark) 0%,
        var(--brown-dark) 20%,
        var(--brown-medium) 40%,
        var(--brown-light) 60%,
        var(--gold-dark) 80%,
        var(--gold-medium) 100%
      );
      background-attachment: fixed;
      color: var(--text-light);
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background:
        radial-gradient(circle at 20% 80%, rgba(241, 222, 168, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(218, 167, 85, 0.06) 0%, transparent 50%);
      pointer-events: none;
      z-index: 0;
    }

    /* Navbar */
    .navbar-custom {
      background: linear-gradient(135deg,
        rgba(26, 15, 10, 0.95) 0%,
        rgba(70, 37, 6, 0.92) 50%,
        rgba(139, 69, 19, 0.95) 100%
      );
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(241, 222, 168, 0.2);
      box-shadow: var(--shadow-strong);
      padding: 16px 0;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .navbar-custom::after {
      content: '';
      position: absolute;
      bottom: -1px;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg,
        transparent,
        var(--gold-medium) 50%,
        transparent
      );
      opacity: 0.5;
    }

    .navbar-brand {
      font-family: 'Playfair Display', serif;
      font-size: 24px;
      font-weight: 700;
      color: var(--gold-lightest) !important;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
      letter-spacing: 0.5px;
    }

    .btn-custom {
      background: linear-gradient(135deg,
        rgba(70, 37, 6, 0.6) 0%,
        rgba(93, 64, 55, 0.6) 100%
      );
      backdrop-filter: blur(10px);
      border: 1px solid rgba(218, 167, 85, 0.3);
      color: var(--gold-lightest);
      padding: 8px 20px;
      border-radius: 10px;
      transition: all 0.3s ease;
      font-weight: 500;
      text-decoration: none;
      display: inline-block;
    }

    .btn-custom:hover {
      background: linear-gradient(135deg,
        var(--gold-dark) 0%,
        var(--gold-medium) 100%
      );
      color: var(--text-dark);
      transform: translateY(-2px);
      box-shadow: var(--shadow-gold);
    }

    .btn-custom i {
      margin-right: 6px;
    }

    /* Main Content */
    .main-content {
      position: relative;
      z-index: 1;
      padding: 40px 0;
    }

    /* Welcome Section */
    .welcome-section {
      background: linear-gradient(135deg,
        rgba(26, 15, 10, 0.8) 0%,
        rgba(70, 37, 6, 0.7) 100%
      );
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 40px;
      margin-bottom: 40px;
      border: 1px solid rgba(241, 222, 168, 0.2);
      box-shadow: var(--shadow-strong);
      animation: slideIn 0.6s ease;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .welcome-section h1 {
      font-family: 'Playfair Display', serif;
      font-size: 36px;
      font-weight: 700;
      color: var(--gold-lightest);
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .welcome-section p {
      color: var(--gold-light);
      font-size: 16px;
      margin: 0;
    }

    /* Dashboard Cards */
    .dashboard-card {
      background: linear-gradient(135deg,
        rgba(45, 24, 16, 0.9) 0%,
        rgba(70, 37, 6, 0.85) 100%
      );
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 30px;
      border: 1px solid rgba(218, 167, 85, 0.3);
      box-shadow: var(--shadow-strong);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      height: 100%;
      position: relative;
      overflow: hidden;
    }

    .dashboard-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg,
        var(--gold-medium),
        var(--gold-light)
      );
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .dashboard-card:hover {
      transform: translateY(-8px);
      border-color: var(--gold-medium);
      box-shadow: 0 12px 40px rgba(218, 167, 85, 0.3);
    }

    .dashboard-card:hover::before {
      opacity: 0.1;
    }

    .card-icon {
      width: 70px;
      height: 70px;
      background: linear-gradient(135deg,
        var(--gold-dark) 0%,
        var(--gold-medium) 100%
      );
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 20px;
      box-shadow: var(--shadow-gold);
      position: relative;
      z-index: 1;
    }

    .card-icon i {
      font-size: 32px;
      color: var(--text-dark);
    }

    .card-title {
      font-family: 'Playfair Display', serif;
      font-size: 22px;
      font-weight: 700;
      color: var(--gold-lightest);
      margin-bottom: 10px;
      position: relative;
      z-index: 1;
    }

    .card-description {
      color: var(--gold-light);
      font-size: 14px;
      margin-bottom: 20px;
      line-height: 1.6;
      position: relative;
      z-index: 1;
    }

    .card-btn {
      background: linear-gradient(135deg,
        var(--gold-dark) 0%,
        var(--gold-medium) 100%
      );
      color: var(--text-dark);
      border: none;
      padding: 12px 24px;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
      position: relative;
      z-index: 1;
    }

    .card-btn:hover {
      background: linear-gradient(135deg,
        var(--gold-medium) 0%,
        var(--gold-lightest) 100%
      );
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(218, 167, 85, 0.4);
    }

    .card-btn i {
      margin-left: 6px;
    }

    /* Quick Actions */
    .quick-actions {
      background: linear-gradient(135deg,
        rgba(26, 15, 10, 0.8) 0%,
        rgba(70, 37, 6, 0.7) 100%
      );
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 30px;
      margin-top: 40px;
      border: 1px solid rgba(241, 222, 168, 0.2);
      box-shadow: var(--shadow-strong);
    }

    .quick-actions h3 {
      font-family: 'Playfair Display', serif;
      font-size: 24px;
      font-weight: 700;
      color: var(--gold-lightest);
      margin-bottom: 20px;
    }

    .action-btn {
      background: linear-gradient(135deg,
        rgba(70, 37, 6, 0.6) 0%,
        rgba(93, 64, 55, 0.6) 100%
      );
      backdrop-filter: blur(10px);
      border: 1px solid rgba(218, 167, 85, 0.3);
      color: var(--gold-lightest);
      padding: 15px 20px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      gap: 15px;
      transition: all 0.3s ease;
      text-decoration: none;
      margin-bottom: 15px;
    }

    .action-btn:hover {
      background: linear-gradient(135deg,
        var(--gold-dark) 0%,
        var(--gold-medium) 100%
      );
      color: var(--text-dark);
      transform: translateX(8px);
      border-color: var(--gold-medium);
      box-shadow: var(--shadow-gold);
    }

    .action-btn i {
      font-size: 24px;
      width: 40px;
      height: 40px;
      background: rgba(218, 167, 85, 0.2);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .action-btn:hover i {
      background: rgba(45, 24, 16, 0.3);
    }

    .action-btn-text {
      flex: 1;
    }

    .action-btn-title {
      font-weight: 600;
      font-size: 15px;
      margin-bottom: 3px;
    }

    .action-btn-desc {
      font-size: 12px;
      opacity: 0.8;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .welcome-section h1 {
        font-size: 28px;
      }

      .dashboard-card {
        margin-bottom: 20px;
      }

      .quick-actions {
        margin-top: 20px;
      }
    }

    /* Loading Animation */
    .card-loading {
      animation: cardPulse 2s ease-in-out infinite;
    }

    @keyframes cardPulse {
      0%, 100% {
        opacity: 1;
      }
      50% {
        opacity: 0.8;
      }
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/admin-dashboard">
        <i class="bi bi-speedometer2"></i> ScentOS Admin
      </a>
      <div class="d-flex gap-2">
        <a href="/admin-chat" class="btn-custom">
          <i class="bi bi-chat-dots-fill"></i> Chat
        </a>
        <a href="/logout" class="btn-custom">
          <i class="bi bi-box-arrow-right"></i> Logout
        </a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container main-content">
    <!-- Welcome Section -->
    <div class="welcome-section">
      <h1><i class="bi bi-gem"></i> Welcome Back, Admin!</h1>
      <p>Manage your luxury fragrance vending system with ease</p>
    </div>

    <!-- Dashboard Cards -->
    <div class="row g-4 mb-4">
      <!-- Products Card -->
      <div class="col-md-4">
        <a href="/produk" style="text-decoration: none;">
          <div class="dashboard-card">
            <div class="card-icon">
              <i class="bi bi-box-seam-fill"></i>
            </div>
            <div class="card-title">Product Management</div>
            <div class="card-description">
              View, add, or update your luxury perfume products catalog
            </div>
            <span class="card-btn">
              Manage Products <i class="bi bi-arrow-right"></i>
            </span>
          </div>
        </a>
      </div>

      <!-- Transactions Card -->
      <div class="col-md-4">
        <a href="/admin-transaksi" style="text-decoration: none;">
          <div class="dashboard-card">
            <div class="card-icon">
              <i class="bi bi-credit-card-fill"></i>
            </div>
            <div class="card-title">Transactions</div>
            <div class="card-description">
              Monitor all sales transactions and payment history
            </div>
            <span class="card-btn">
              View Transactions <i class="bi bi-arrow-right"></i>
            </span>
          </div>
        </a>
      </div>

      <!-- Users Card -->
      <div class="col-md-4">
        <a href="/kelola-user" style="text-decoration: none;">
          <div class="dashboard-card">
            <div class="card-icon">
              <i class="bi bi-people-fill"></i>
            </div>
            <div class="card-title">User Management</div>
            <div class="card-description">
              Manage customer accounts and technician profiles
            </div>
            <span class="card-btn">
              Manage Users <i class="bi bi-arrow-right"></i>
            </span>
          </div>
        </a>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="quick-actions">
      <h3><i class="bi bi-lightning-fill"></i> Quick Actions</h3>
      <div class="row">
        <div class="col-md-6">
          <a href="/produk" class="action-btn">
            <i class="bi bi-plus-circle-fill"></i>
            <div class="action-btn-text">
              <div class="action-btn-title">Add New Product</div>
              <div class="action-btn-desc">Add a new perfume to your catalog</div>
            </div>
            <i class="bi bi-chevron-right"></i>
          </a>
        </div>
        <div class="col-md-6">
          <a href="/analytics" class="action-btn">
            <i class="bi bi-graph-up"></i>
            <div class="action-btn-text">
              <div class="action-btn-title">View Analytics</div>
              <div class="action-btn-desc">Check real-time sales and revenue reports</div>
            </div>
            <i class="bi bi-chevron-right"></i>
          </a>
        </div>
        <div class="col-md-6">
          <a href="/kelola-user" class="action-btn">
            <i class="bi bi-person-plus-fill"></i>
            <div class="action-btn-text">
              <div class="action-btn-title">Add New User</div>
              <div class="action-btn-desc">Create new customer or technician account</div>
            </div>
            <i class="bi bi-chevron-right"></i>
          </a>
        </div>
        <div class="col-md-6">
          <a href="/admin-chat" class="action-btn">
            <i class="bi bi-chat-left-dots-fill"></i>
            <div class="action-btn-text">
              <div class="action-btn-title">Customer Support</div>
              <div class="action-btn-desc">Respond to customer inquiries</div>
            </div>
            <i class="bi bi-chevron-right"></i>
          </a>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Add loading animation on card click
    document.querySelectorAll('.dashboard-card, .action-btn').forEach(card => {
      card.addEventListener('click', function() {
        this.classList.add('card-loading');
      });
    });
  </script>

</body>
</html>
