<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Reset Password</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body{font-family:Poppins, sans-serif;background:#fafafa;display:flex;justify-content:center;align-items:center;height:100vh;margin:0}
    .box{background:#fff;padding:40px;border-radius:12px;box-shadow:0 10px 25px rgba(0,0,0,.1);width:340px;text-align:center}
    input{width:100%;padding:12px;margin:12px 0;border:1px solid #ccc;border-radius:8px}
    button{width:100%;padding:12px;background:#FFD54F;border:0;border-radius:8px;font-weight:600;cursor:pointer}
    button:hover{background:#ffe082}
    .msg{margin-top:10px;color:green}
  </style>
</head>
<body>
  <div class="box">
    <h2>Password Baru</h2>
    <% if (!msg) { %>
      <form method="POST" action="/reset-password">
        <input type="hidden" name="token" value="<%= token %>">
        <input type="password" name="password" placeholder="Password baru" required>
        <button type="submit">Perbarui</button>
      </form>
    <% } else { %>
      <p class="msg"><%= msg %></p>
      <a href="/login">Ke halaman login</a>
    <% } %>
  </div>
</body>
</html>
